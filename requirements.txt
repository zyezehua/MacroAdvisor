# Runtime dependencies for the data pipeline + Phase-1 dashboard (kept lean so
# Streamlit Cloud builds fast). Phase-2 analytics libs live in requirements-phase2.txt.

# --- Core data layer ---
pandas>=2.2
numpy>=1.26
pyarrow>=15.0
requests>=2.31
PyYAML>=6.0
yfinance>=0.2.40
lxml>=5.0          # pandas.read_html for index constituents

# --- Dashboard ---
streamlit>=1.36
plotly>=5.22

# --- Remote cache (deployment) ---
huggingface_hub>=0.23
